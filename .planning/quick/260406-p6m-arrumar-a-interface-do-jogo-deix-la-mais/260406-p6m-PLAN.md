---
phase: 260406-p6m-arrumar-a-interface-do-jogo-deix-la-mais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/CampaignMap.tsx
  - frontend/src/components/HUD.tsx
  - frontend/src/components/GameOverlay.tsx
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/ShopScreen.tsx
  - frontend/src/styles/index.css
autonomous: false
requirements:
  - CORE-01
must_haves:
  truths:
    - The main shell feels minimalist and futuristic instead of default or flat.
    - HUD, campaign map, shop, settings, and runtime overlays stay readable over the canvas on mobile and desktop widths.
    - The refreshed interface keeps strong contrast, clear hierarchy, and tap-friendly spacing on small screens.
    - Game mechanics, input handling, and state transitions remain unchanged.
  artifacts:
    - path: frontend/src/App.tsx
      provides: Shell layout, canvas framing, and UI stacking
    - path: frontend/src/components/CampaignMap.tsx
      provides: Campaign selector visual skin
    - path: frontend/src/components/HUD.tsx
      provides: Top status bar and boss health presentation
    - path: frontend/src/components/GameOverlay.tsx
      provides: In-canvas overlays and modal states
    - path: frontend/src/components/SettingsPanel.tsx
      provides: Settings modal visual treatment
    - path: frontend/src/components/ShopScreen.tsx
      provides: Shop modal visual treatment
    - path: frontend/src/styles/index.css
      provides: Global background, typography, and responsive presentation rules
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/components/GameOverlay.tsx
      via: Layered absolute positioning above the canvas
      pattern: "absolute|z-"
    - from: frontend/src/styles/index.css
      to: frontend/src/components/HUD.tsx
      via: Shared visual tokens, spacing, and contrast rules
      pattern: "max-width|backdrop|contrast"
    - from: frontend/src/components/SettingsPanel.tsx
      to: viewport
      via: Mobile-safe modal sizing and scroll containment
      pattern: "max-h|overflow-y-auto"
---

<objective>
Refresh the game interface into a cleaner minimalist-futuristic presentation without changing gameplay mechanics.

Purpose: Improve first-impression quality, mobile readability, and overlay clarity while keeping the web build stable.
Output: Updated shell layout, component skins, and global styles that make the UI feel more dynamic and polished.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/quick.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/components/CampaignMap.tsx
@frontend/src/components/HUD.tsx
@frontend/src/components/GameOverlay.tsx
@frontend/src/components/SettingsPanel.tsx
@frontend/src/components/ShopScreen.tsx
@frontend/src/styles/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the shell and atmosphere</name>
  <files>frontend/src/App.tsx, frontend/src/styles/index.css</files>
  <action>Rework the top-level shell into a more intentional cockpit-like frame with a stronger visual hierarchy, atmospheric background treatment, and better spacing around the canvas. Keep the game canvas, settings trigger, and status composition intact; do not alter any engine calls, state transitions, or game rules.</action>
  <verify>npm run typecheck && npm run build</verify>
  <done>The app shell reads as a designed scene rather than a default container, the canvas still fills responsively, and gameplay behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the in-game surfaces</name>
  <files>frontend/src/components/CampaignMap.tsx, frontend/src/components/HUD.tsx, frontend/src/components/GameOverlay.tsx</files>
  <action>Apply a consistent minimalist-futuristic skin to the campaign selector, HUD, and runtime overlays: tighten typography, improve card and pill contrast, refine progress indicators, and make the overlay states easier to scan on top of the canvas. Preserve the existing props, handlers, and copy flow exactly; only change presentation and layout behavior.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <done>The campaign map, HUD, and runtime overlays look cohesive, remain legible over the playfield, and still expose the same controls and states.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify mobile readability and modal fit</name>
  <files>frontend/src/components/SettingsPanel.tsx, frontend/src/components/ShopScreen.tsx</files>
  <action>After the visual refresh is implemented, inspect the settings and shop modals in the browser at mobile and desktop sizes. Confirm that controls do not clip, the modal content remains readable over the canvas, scrolling works when content exceeds the viewport, and the refreshed style stays consistent with the rest of the interface.</action>
  <verify>npm run build</verify>
  <done>The settings and shop surfaces fit safely on small screens, remain readable over the canvas, and do not introduce any clipped or unreachable controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewport -> layout | Untrusted screen sizes and aspect ratios can expose clipping or overlap if the responsive shell is not constrained correctly. |
| runtime game state -> UI copy | Boss, score, and progress values are rendered into overlays and must be handled safely so layout does not break on long or unexpected values. |
| user taps -> modal actions | Button and overlay interactions must remain semantically unchanged while the visual treatment changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | frontend/src/App.tsx and overlay components | mitigate | Keep existing typed props and handlers, clamp visible progress widths, and preserve current action flow so the redesign cannot alter gameplay behavior. |
| T-quick-02 | D | responsive shell and modals | accept | Visual clipping on unsupported viewport extremes is low risk for this UI refresh and will be caught by the mobile readability check. |
| T-quick-03 | I | SettingsPanel and ShopScreen | accept | No new data exposure is introduced; the work only changes presentation of already-visible local state. |
</threat_model>

<verification>
- Automated checks must pass: typecheck, lint, and production build.
- The refreshed UI must remain readable at small mobile widths and over the canvas.
- Settings and shop panels must stay scroll-safe and tap-friendly.
- No gameplay mechanics, input bindings, or state transitions may change.
</verification>

<success_criteria>
- The app presents a deliberate minimalist-futuristic visual language across the shell, HUD, campaign map, and overlays.
- Mobile layouts stay legible and usable without clipping critical controls or text.
- The interface feels more dynamic and polished without any game logic changes.
- The web build still passes the standard quality gates.
</success_criteria>

<output>
After completion, create .planning/quick/260406-p6m-arrumar-a-interface-do-jogo-deix-la-mais/260406-p6m-SUMMARY.md
</output>

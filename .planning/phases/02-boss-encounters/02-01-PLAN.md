---
phase: 02-boss-encounters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-boss-encounters/02-VALIDATION.md
  - frontend/src/game/types.ts
  - frontend/src/state/gameStore.ts
  - frontend/src/game/config/gameplay.ts
  - frontend/src/game/systems/progressionSystem.ts
  - frontend/src/game/engine.ts
  - frontend/src/game/config/stages.ts
  - frontend/src/game/config/bossProfiles.ts
autonomous: true
requirements:
  - CAMP-02
must_haves:
  truths:
    - "Toda fase de campanha (1-5) apresenta encontro de boss antes da transicao"
    - "Bosses de fases consecutivas possuem padroes mecanicos distinguiveis"
    - "Resultado do encontro de boss (em andamento/vitoria/derrota) fica explicito no estado de sessao"
  artifacts:
    - path: "frontend/src/game/config/bossProfiles.ts"
      provides: "Perfis data-driven por fase para identidade mecanica"
    - path: "frontend/src/game/systems/progressionSystem.ts"
      provides: "Regra de progressao com boss obrigatorio na campanha"
    - path: "frontend/src/game/types.ts"
      provides: "Contrato de estado de encontro de boss com attempt/outcome/timestamps"
  key_links:
    - from: "frontend/src/game/engine.ts"
      to: "frontend/src/game/systems/progressionSystem.ts"
      via: "evaluateStageProgression para transicao enemy->boss->shop"
      pattern: "evaluateStageProgression"
    - from: "frontend/src/game/config/bossProfiles.ts"
      to: "frontend/src/game/config/stages.ts"
      via: "composicao de dificuldade base + identidade mecanica"
      pattern: "stage"
---

<objective>
Implementar o contrato base de encounters de boss da fase 2: boss por fase de campanha, identidade mecanica por perfil e ciclo de estado explicito para tentativa/resultado.

Purpose: criar a fundacao de CAMP-02 sem mexer em arquitetura ampla.
Output: regras de progressao atualizadas, perfis de boss por fase, estado de encounter consistente no store e artefato Nyquist inicial da fase.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-boss-encounters/02-RESEARCH.md
@.planning/phases/02-boss-encounters/02-VALIDATION.md
@.planning/phases/01-campaign-meta-layer/01-UI-SPEC.md
@frontend/src/game/engine.ts
@frontend/src/game/types.ts
@frontend/src/game/systems/progressionSystem.ts
@frontend/src/game/config/stages.ts

<interfaces>
From frontend/src/game/types.ts:
```ts
export type BossEncounterState = {
  active: boolean
  bossId: string | null
  health: number
  maxHealth: number
}
```

From frontend/src/game/systems/progressionSystem.ts:
```ts
export function evaluateStageProgression(
  currentStage: number,
  enemiesRemaining: number,
  bossActive = true,
  bossDefeated = true
): ProgressionResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Estabelecer artefato Nyquist da fase 02</name>
  <files>.planning/phases/02-boss-encounters/02-VALIDATION.md</files>
  <action>
    Criar ou atualizar `02-VALIDATION.md` com frontmatter de fase e secoes minimas definidas na arquitetura de validacao da pesquisa:
    - framework/comandos (`test`, `validate`)
    - sampling rate por task/wave
    - matriz de verificacao por task para 02-01 (comandos automatizados)
    - checklist de sign-off com `nyquist_compliant: false` inicialmente.

    O artefato deve ser tratado como fonte de verdade de validacao para os planos 02-02 e 02-03.
  </action>
  <verify>
    <automated>rg -n "nyquist_compliant|Per-Task Verification Map|Sampling Rate" .planning/phases/02-boss-encounters/02-VALIDATION.md</automated>
  </verify>
  <done>`02-VALIDATION.md` existe com estrutura Nyquist e itens de verificacao de 02-01 preenchidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Definir contratos de encounter e perfis de boss por fase</name>
  <files>frontend/src/game/types.ts, frontend/src/state/gameStore.ts, frontend/src/game/config/bossProfiles.ts</files>
  <behavior>
    - Test 1: estado inicial contem `stage`, `attempt`, `startedAtMs`, `endedAtMs`, `outcome`, `damageTaken` em `bossEncounter`.
    - Test 2: cada fase 1..5 tem perfil com sequencia de ataques e parametros de movimento/telegraph distintos.
  </behavior>
  <action>
    Evoluir `BossEncounterState` para incluir dados de lifecycle de encounter: `stage`, `attempt`, `startedAtMs`, `endedAtMs`, `outcome` e `damageTaken`.

    Criar `frontend/src/game/config/bossProfiles.ts` com perfis data-driven para fases 1..5, implementando arquétipo principal + variacao media por tentativa (per D-01 e D-03), contendo no minimo:
    - `stage`
    - `attackPatternSequence`
    - `telegraphMs`
    - `movementModel`
    - `feedbackPreset`

    Inicializar o novo shape em `gameStore` sem quebrar os campos existentes.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Contratos de estado e perfis por fase existem e sao consumiveis pela engine sem uso de `any`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar regra boss-every-stage na campanha e lifecycle no loop</name>
  <files>frontend/src/game/config/gameplay.ts, frontend/src/game/systems/progressionSystem.ts, frontend/src/game/engine.ts, frontend/src/game/config/stages.ts</files>
  <behavior>
    - Test 1: ao limpar inimigos da fase 1..5, a progressao entra em boss fight antes de loja.
    - Test 2: ao derrotar boss, `outcome` vira `victory` e transicao para `shop` preserva consistencia.
    - Test 3: ao perder durante boss ativo, `outcome` vira `defeat` com `endedAtMs` preenchido.
  </behavior>
  <action>
    Ajustar progressao para campanha curta (1..5) com boss obrigatorio em cada fase sem remover comportamento de ciclo para fases fora da campanha, mantendo tom de dificuldade normal com pressao quase bullet-hell porem justa (per D-02 e D-04).

    No `engine.ts`, introduzir helpers dedicados de lifecycle:
    - `startBossEncounter`
    - `handleBossVictory`
    - `handleBossPlayerDefeat`

    Esses helpers devem atualizar `bossEncounter` (attempt/outcome/timestamps/stage) em um unico ponto de verdade, preservando boss defeat como gatilho de progressao e retry imediato no mesmo stage (per D-05 e D-06).

    Ajustar `stages.ts` para manter curva monotona de dificuldade entre fases 1..5 sem picos abruptos de HP/cadencia e sem dependencias online no loop (per D-07).
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- tests/unit/us2-boss-phase-detection.test.ts tests/integration/us2-phase-transition-difficulty.test.tsx</automated>
  </verify>
  <done>Campanha 1..5 sempre entra em boss encounter e o estado de resultado do boss e refletido corretamente no store.</done>
</task>

</tasks>

<verification>
Estrategia de teste deste plano:
- Estabelecer `02-VALIDATION.md` como artefato Nyquist da fase com mapa por task.
- Atualizar testes unitarios de regra de boss por fase e perfis por stage.
- Atualizar integracao de transicao de fase para fluxo boss obrigatorio.
- Gate automatizado do plano: `npm --prefix frontend run test -- tests/unit/us2-boss-phase-detection.test.ts tests/integration/us2-phase-transition-difficulty.test.tsx && npm --prefix frontend run typecheck`.
</verification>

<success_criteria>
- CAMP-02 parcialmente atendido com boss encounter em todas as fases 1..5.
- Estado de lifecycle do encounter persiste no store para uso de UI/telemetria.
- Curva 1..5 continua monotona e sem salto injusto evidente no contrato de dados.
- Artefato Nyquist da fase 02 fica criado e pronto para atualizacao incremental nos planos seguintes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-boss-encounters/02-01-SUMMARY.md`
</output>



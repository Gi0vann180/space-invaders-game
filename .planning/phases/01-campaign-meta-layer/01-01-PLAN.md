---
phase: 01-campaign-meta-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/game/types.ts
  - frontend/src/state/gameStore.ts
  - frontend/src/services/shopPersistenceService.ts
  - frontend/src/game/engine.ts
  - frontend/tests/integration/us1-boss-stage-flow.test.tsx
autonomous: true
requirements:
  - CAMP-05
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - "Player progress persists across app reopen"
    - "Paused or interrupted run can be resumed safely"
    - "Core campaign progression works fully offline"
  artifacts:
    - path: "frontend/src/services/shopPersistenceService.ts"
      provides: "Campaign progress persistence and migration-safe profile read/write"
    - path: "frontend/src/game/engine.ts"
      provides: "Checkpoint-based save triggers and resume/restart flow wiring"
    - path: "frontend/src/state/gameStore.ts"
      provides: "Runtime session snapshot fields for interrupted run handling"
  key_links:
    - from: "frontend/src/game/engine.ts"
      to: "frontend/src/services/shopPersistenceService.ts"
      via: "checkpoint writes on phase start/completion/pause"
      pattern: "savePersistedProgressProfile"
    - from: "frontend/src/game/engine.ts"
      to: "frontend/src/state/gameStore.ts"
      via: "resume-restart branch updates"
      pattern: "gameStore\.setState"
---

<objective>
Implement campaign persistence foundation and resume-safe runtime contracts for Phase 01.

Purpose: lock deterministic offline progression behavior before campaign map UI work.
Output: migration-safe persisted profile model, checkpoint save flow, and test coverage for resume/offline behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-campaign-meta-layer/01-CONTEXT.md
@.planning/phases/01-campaign-meta-layer/01-UI-SPEC.md
@.planning/phases/01-campaign-meta-layer/01-RESEARCH.md
@.planning/phases/01-campaign-meta-layer/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend campaign session and persistence contracts</name>
  <files>frontend/src/game/types.ts, frontend/src/state/gameStore.ts, frontend/src/services/shopPersistenceService.ts</files>
  <read_first>frontend/src/game/types.ts, frontend/src/state/gameStore.ts, frontend/src/services/shopPersistenceService.ts, .planning/phases/01-campaign-meta-layer/01-CONTEXT.md</read_first>
  <behavior>
    - Test: legacy profile with only highestUnlockedStage+totalRuns is read with safe defaults for new campaign fields.
    - Test: saving progress profile preserves minimum fields required by D-09 to D-16.
  </behavior>
  <action>
    Add campaign snapshot fields to runtime and persistence contracts using concrete keys:
    - `lastAttemptedStage: number | null`
    - `lastCompletedStage: number | null`
    - `interruptedRun: { stage: number; atStatus: 'running' | 'paused'; savedAt: string } | null`

    In `shopPersistenceService.ts`, implement additive migration logic:
    - if stored record misses new keys, hydrate with null defaults, never throw.
    - clamp `highestUnlockedStage >= 1` and `totalRuns >= 0` as today.

    In `gameStore.ts`, initialize new campaign fields in initial state and preserve progression profile through reset behavior.
  </action>
  <acceptance_criteria>
    - `frontend/src/game/types.ts` contains `lastAttemptedStage` and `interruptedRun` campaign fields.
    - `frontend/src/services/shopPersistenceService.ts` includes fallback defaults for missing campaign keys.
    - `frontend/src/state/gameStore.ts` initializes campaign fields without breaking existing status values.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Types and persistence model support migration-safe campaign checkpoints with no runtime type regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire checkpoint saves and interrupted-run flow in engine</name>
  <files>frontend/src/game/engine.ts, frontend/src/services/shopPersistenceService.ts</files>
  <read_first>frontend/src/game/engine.ts, frontend/src/services/shopPersistenceService.ts, .planning/phases/01-campaign-meta-layer/01-CONTEXT.md</read_first>
  <behavior>
    - Test: phase start writes `lastAttemptedStage` checkpoint.
    - Test: boss defeat writes `lastCompletedStage` and unlocks next stage.
    - Test: pause/background writes interrupted run payload.
    - Test: invalid interrupted payload falls back to start of current stage.
  </behavior>
  <action>
    In `engine.ts`, add concrete checkpoint write calls on these events:
    - startRound -> save `lastAttemptedStage = current stage`, clear stale interrupted state.
    - enterShop after boss defeat -> save `lastCompletedStage = current stage`, bump `highestUnlockedStage` to `current+1` when higher.
    - pauseGame and visibility-pause path -> save `interruptedRun` payload with stage, status, timestamp.

    Add a deterministic recovery helper in persistence service:
    - validate interrupted payload shape.
    - return null on invalid payload and expose fallback path to start current stage.
  </action>
  <acceptance_criteria>
    - `frontend/src/game/engine.ts` contains explicit save calls for start, completion, and pause checkpoints.
    - `frontend/src/services/shopPersistenceService.ts` contains interrupted payload validator or equivalent guard.
    - no checkpoint write relies on periodic timers.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- frontend/tests/integration/us1-boss-stage-flow.test.tsx</automated>
  </verify>
  <done>Engine writes campaign progress on required checkpoints and recovers safely from invalid interrupted snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration coverage for persistence and resume flow</name>
  <files>frontend/tests/integration/us1-boss-stage-flow.test.tsx</files>
  <read_first>frontend/tests/integration/us1-boss-stage-flow.test.tsx, frontend/src/game/engine.ts, frontend/src/services/shopPersistenceService.ts</read_first>
  <behavior>
    - Test interrupted run prompt preconditions are represented by persisted interrupted payload.
    - Test phase completion persists unlock progression across simulated reopen.
    - Test offline path does not require network calls for campaign progression state.
  </behavior>
  <action>
    Expand integration spec to include deterministic cases for:
    - legacy profile migration defaults.
    - checkpoint write sequence (start, pause, completion).
    - fallback to stage start when interrupted payload is malformed.

    Keep tests isolated to campaign state and do not assert UI rendering here.
  </action>
  <acceptance_criteria>
    - integration file contains explicit test cases named for migration, checkpoint, and fallback behavior.
    - tests assert persisted keys `highestUnlockedStage`, `lastAttemptedStage`, `lastCompletedStage`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- frontend/tests/integration/us1-boss-stage-flow.test.tsx && npm run typecheck</automated>
  </verify>
  <done>Integration coverage protects campaign persistence contracts needed by phase requirements CAMP-05, CORE-02, CORE-03.</done>
</task>

</tasks>

<verification>
Run focused campaign integration tests and typecheck after each task; run full validate at plan end.
</verification>

<success_criteria>
- Campaign checkpoints are persisted on phase start, completion, and pause/background.
- Interrupted run payload is recoverable and invalid payloads fallback safely.
- Offline progression data path is fully local and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-campaign-meta-layer/01-01-SUMMARY.md`
</output>

---
phase: 01-campaign-meta-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/CampaignMap.tsx
  - frontend/src/components/GameOverlay.tsx
  - frontend/src/components/HUD.tsx
  - frontend/tests/integration/us1-boss-stage-flow.test.tsx
autonomous: true
requirements:
  - CAMP-01
  - CORE-02
must_haves:
  truths:
    - "Player sees a 1-5 campaign map with locked, unlocked, and completed states"
    - "Boss completion shows phase-complete feedback with reward and next action"
    - "Interrupted run resume path presents Continue or Restart choice"
  artifacts:
    - path: "frontend/src/components/CampaignMap.tsx"
      provides: "Campaign map UI with node state rendering and lock hints"
    - path: "frontend/src/App.tsx"
      provides: "Campaign map and resume prompt wiring to engine/store state"
    - path: "frontend/src/components/GameOverlay.tsx"
      provides: "Completion and interrupted-run action affordances"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/CampaignMap.tsx"
      via: "props mapping from game store progression state"
      pattern: "CampaignMap"
    - from: "frontend/src/components/GameOverlay.tsx"
      to: "frontend/src/game/engine.ts"
      via: "continue/restart action handlers"
      pattern: "onPrimaryAction|onResume|onRestart"
---

<objective>
Implement campaign map and progression UI wiring that satisfies Phase 01 visual and interaction contracts.

Purpose: deliver observable player-facing progression UX for CAMP-01 while reusing persistence contracts from Plan 01.
Output: campaign map component, completion/restart flow UI, integration coverage for campaign state rendering.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-campaign-meta-layer/01-CONTEXT.md
@.planning/phases/01-campaign-meta-layer/01-UI-SPEC.md
@.planning/phases/01-campaign-meta-layer/01-RESEARCH.md
@.planning/phases/01-campaign-meta-layer/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build campaign map UI component and state model</name>
  <files>frontend/src/components/CampaignMap.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx, frontend/src/components/HUD.tsx, .planning/phases/01-campaign-meta-layer/01-UI-SPEC.md, .planning/phases/01-campaign-meta-layer/01-CONTEXT.md</read_first>
  <behavior>
    - Test: map renders phases 1-5 with explicit locked/unlocked/completed states.
    - Test: locked nodes remain visible with lock hint text.
    - Test: completed nodes remain replayable.
  </behavior>
  <action>
    Create `CampaignMap` component with concrete props:
    - `highestUnlockedStage: number`
    - `currentStage: number`
    - `completedStages: number[]`
    - `onSelectStage(stage: number): void`

    Render linear connected nodes for stages 1..5 with state rules:
    - locked: stage > highestUnlockedStage
    - unlocked: stage <= highestUnlockedStage and not completed
    - completed: stage in completedStages

    Use UI-SPEC values:
    - spacing tokens from 4/8/16/24/32/48/64 scale
    - heading/body/label/display typography limits
    - color contract with dominant/secondary/accent and lock hints.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/CampaignMap.tsx` exists and exports `CampaignMap`.
    - component includes explicit rendering for stages 1 through 5.
    - lock hint copy is present: "Derrote o chefe da fase anterior para desbloquear".
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- frontend/tests/integration/us1-boss-stage-flow.test.tsx</automated>
  </verify>
  <done>Campaign map UI supports required node states and selection behavior for CAMP-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire completion and interrupted-run prompts in overlay/app flow</name>
  <files>frontend/src/App.tsx, frontend/src/components/GameOverlay.tsx, frontend/src/components/HUD.tsx</files>
  <read_first>frontend/src/App.tsx, frontend/src/components/GameOverlay.tsx, frontend/src/components/HUD.tsx, .planning/phases/01-campaign-meta-layer/01-UI-SPEC.md</read_first>
  <behavior>
    - Test: completion state displays "FASE COMPLETADA" with reward summary and next action.
    - Test: interrupted-run reopen displays binary prompt Continue or Restart.
    - Test: retry path remains immediate on defeat.
  </behavior>
  <action>
    Extend overlay/app flow with concrete UI states:
    - completion modal title `FASE COMPLETADA` with reward summary text and CTA `Proxima fase`.
    - interrupted prompt title `Run interrompida encontrada` with buttons `Continuar` and `Reiniciar`.
    - restart confirm copy: `Reiniciar fase: Reiniciar agora? O progresso da run atual sera perdido.`

    Keep existing pause/game-over flow intact and route actions to engine handlers from Plan 01 contracts.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/GameOverlay.tsx` contains copy strings from UI-SPEC for completion and interrupted run.
    - `frontend/src/App.tsx` wires Continue and Restart handlers for interrupted-run branch.
    - no removal of existing pause/resume actions.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- frontend/tests/integration/us1-boss-stage-flow.test.tsx && npm run typecheck</automated>
  </verify>
  <done>Player can understand progression outcomes and safely choose continue/restart after interruption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add campaign UI integration assertions</name>
  <files>frontend/tests/integration/us1-boss-stage-flow.test.tsx</files>
  <read_first>frontend/tests/integration/us1-boss-stage-flow.test.tsx, frontend/src/components/CampaignMap.tsx, frontend/src/components/GameOverlay.tsx</read_first>
  <behavior>
    - Test campaign map node statuses and lock hints render from progression data.
    - Test completion overlay and interrupted-run prompts show exact copy contracts.
    - Test replay of completed stage does not reduce unlocked progression.
  </behavior>
  <action>
    Extend integration test file with UI-facing campaign assertions tied to store snapshots.
    Keep deterministic fixtures and avoid brittle visual snapshot testing.
  </action>
  <acceptance_criteria>
    - integration file includes assertions for map states (locked/unlocked/completed).
    - integration file asserts `FASE COMPLETADA`, `Continuar`, and `Reiniciar` labels.
    - integration file includes replay-without-regression case.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- frontend/tests/integration/us1-boss-stage-flow.test.tsx && npm run validate</automated>
  </verify>
  <done>Campaign progression UI behavior is covered and aligned to CONTEXT + UI-SPEC decisions.</done>
</task>

</tasks>

<verification>
Run integration tests after each task and full validate at the end of plan execution.
</verification>

<success_criteria>
- Campaign map with 1-5 nodes correctly reflects progression state.
- Completion and interrupted-run flows use locked copy and actions.
- Replay and retry UX remains consistent with phase decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-campaign-meta-layer/01-02-SUMMARY.md`
</output>
